import 'package:flutter/material.dart';
class UsrSearch extends StatelessWidget {
  const UsrSearch ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:  Center(child: Text('Search')),
    );
  }
}